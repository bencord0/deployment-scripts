[Service]
ExecStart=
ExecStart=/usr/bin/kube-controller-manager --kubeconfig /etc/kubernetes/kubeconfig --service-account-private-key-file /etc/kubernetes/server.key --root-ca-file /etc/kubernetes/ca.pem
Restart=on-failure
