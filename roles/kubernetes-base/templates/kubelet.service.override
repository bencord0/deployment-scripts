[Service]
ExecStart=
ExecStart=/usr/bin/kubelet --pod-manifest-path=/etc/kubernetes/manifests --require-kubeconfig --kubeconfig=/etc/kubernetes/kubeconfig --node-ip=${MYIP} --cluster-dns=${MYIP}
Restart=on-failure
